[user]
	email = ezhang887@gmail.com
	name = Eric Zhang

[core]
	editor = vim
	excludesfile = ~/.gitignore_global

[credential]
	helper = cache --timeout=3600

[url "git@github.com:"]
        insteadOf = https://github.com/
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
